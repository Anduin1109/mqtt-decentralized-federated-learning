import torch

def FedAvg(tensors, weights, performance):
    '''
    Federated Averaging
    :param tensors: list of tensors
    :param weights: list of floats
    :param performance: list of floats
    :return: tensor
    '''
    # to be implemented later
    return torch.zeros_like(tensors[0])